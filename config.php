<?
    // define('DB_HOST', 'localhost');
    // define('DB_NAME', '');
    // define('DB_USER', '');
    // define('DB_PASS', 'admin');

    if (!defined('DB_HOST')) {
        define('DB_HOST', 'localhost');
      }
      
      if (!defined('DB_NAME')) {
        define('DB_NAME', 'db_movie');
      }
      
      if (!defined('DB_USER')) {
        define('DB_USER', 'admin');
      }
      
      if (!defined('DB_PASS')) {
        define('DB_PASS', 'admin');
      }
?>